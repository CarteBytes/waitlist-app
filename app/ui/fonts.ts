import { Oswald, DynaPuff } from "next/font/google";

export const oswald = Oswald({ subsets: ["latin"] });
export const dynaPuff = DynaPuff({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});
